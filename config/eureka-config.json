{
    "version": 1,

    "properties": {
        "eureka_base_url": "http://localhost:8761/eureka",
        "environment": "desarrollo"
    },

    "eureka": {
        "instance-hostname-pattern": "{{service_name}}-{{environment}}.osrouter.dev.corpme.int",
        "register-url-pattern": "{{eureka_base_url}}/apps/{{service_name}}",
        "unregister-url-pattern": "{{eureka_base_url}}/apps/{{service_name}}/PARADTRA04.corpme.es:{{service_name}}"
    }
}
