{
    "version": 1,

    "properties": {
        "eureka_base_url": "http://localhost:8761/eureka",
        "environment": "desarrollo"
    },

    "eureka": {
        "register-url": "{{eureka_base_url}}/apps/{{service_name}}",
        "unregister-url": "{{eureka_base_url}}/apps/{{service_name}}/PARADTRA04.corpme.es:{{service_name}}"
    }
}
